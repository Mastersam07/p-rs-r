name: flutter_whatsapp_chat_parser
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  file_picker: ^4.6.1
  intl: ^0.18.0
  archive: ^3.1.2
  mime: ^1.0.0
  path_provider: ^2.0.2
  just_audio: ^0.9.2
  audio_session: ^0.1.5
  url_launcher: ^6.0.9
  open_file: ^3.2.1
  video_player: ^2.1.10
  google_fonts: ^3.0.1
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
